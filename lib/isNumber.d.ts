/**
 * isNumber 判断是否是number类型
 * @version 1.0.1
 * @param val 要判断的变量
 * @category isNumber
 * @example
 * ``` typescript
 * isNumber(123)
 * ```
 */
declare const isNumber: (val: unknown) => boolean;
export default isNumber;
